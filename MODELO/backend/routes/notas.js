const express = require('express');
const router = express.Router();
const db = require('./db');

// Todas las rutas ya están incluidas en server.js
// Este archivo se mantiene para estructura modular

module.exports = router;